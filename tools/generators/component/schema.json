{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "id": "component",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the component.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the component?"
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "alias": "p",
      "x-prompt": "What is the name of the project to add the component to?"
    },
    "directory": {
      "type": "string",
      "description": "The directory at which to create the component file, relative to the project root.",
      "alias": "dir"
    },
    "export": {
      "type": "boolean",
      "description": "When true, the component will be exported from the project index.ts file.",
      "default": true
    }
  },
  "required": ["name", "project"]
}
